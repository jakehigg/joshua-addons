# Secrets and settings for the `pantry` addon. Copy this file to `.env` in
# this directory before you run `make up ADDON=pantry` or `make up-dev ADDON=pantry`.

# The release of the pantry image compose runs. Set it to take another
# release. The releases are at https://github.com/jakehigg/joshua-addons/releases
JOSHUA_ADDONS_VERSION=0.1.3

# The compose network joshua-ai created. Set this only when joshua-ai's
# project name is not the default `joshua-ai` (the network would then not be
# `joshua-ai_default`).
#JOSHUA_NETWORK=joshua-ai_default

# When set, every request to /mcp needs `Authorization: Bearer <ADDON_TOKEN>`.
# Set the same value here and for PANTRY_ADDON_TOKEN in joshua-ai's .env.
ADDON_TOKEN=

# A full Postgres URL (e.g. postgresql://user:pass@host:5432/pantry). When
# set, this addon uses Postgres instead of the bundled SQLite file, and the
# `pantry-data` volume goes unused.
#DATABASE_URL=

# Where the SQLite database file lives, when DATABASE_URL is not set. Matches
# the `pantry-data` volume mount; only change this if you also change the
# volume mount in docker-compose.yml.
#PANTRY_DB=/data/pantry.db

# The log level: DEBUG, INFO, WARNING, ERROR, or CRITICAL. The default is INFO.
#LOG_LEVEL=INFO

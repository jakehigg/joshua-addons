[project]
name = "joshua-pantry"
version = "0.0.0"
requires-python = ">=3.13"
license = "AGPL-3.0-or-later"
dependencies = [
    "sqlalchemy[asyncio]>=2.0.51",
    "aiosqlite>=0.20",
    "asyncpg>=0.30",
    "pydantic>=2.9",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

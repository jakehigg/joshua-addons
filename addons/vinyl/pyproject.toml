[project]
name = "joshua-vinyl"
version = "0.0.0"
requires-python = ">=3.13"
license = "AGPL-3.0-or-later"
dependencies = [
    "httpx>=0.27",
    "mcp>=1.2",
    "pydantic>=2.9",
    "starlette>=0.37",
    "uvicorn>=0.30",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
